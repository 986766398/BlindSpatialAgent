# BlindSpatialAgent v0.3 最终交付报告

> 对应任务书**第三十四节**要求的十项内容。
> 阅读顺序：先看 `docs/V02_ARCHITECTURE_AUDIT.md`（为什么改），
> 再看 `docs/V03_MIGRATION_LOG.md`（逐阶段改了什么），最后看本报告（结论与验收）。

| 项目 | 内容 |
| --- | --- |
| 版本 | `v0.3.0`（`config/config.yaml:system.version`，由 `0.2.0` 升级） |
| 代码规模 | 约 **21,900 行** Python（不含 `.venv`），跨 13 个包 |
| 技术栈 | Python 3.13 / FastAPI / WebSocket / Pydantic v2 / OpenCV / SwiftUI |
| 交付形态 | 可运行原型（**不接真实硬件**） + 完整文档 + 可复现验收证据 |
| 验收结论 | **通过** —— 自检 74/74、验收场景 10/10、七拍验收 Demo 7/7、Gate 1~10 全部满足 |

---

## 1. v0.2 原架构

v0.2 是一个**能跑通「模拟环境 + 真实 iPhone 摄像头 + 真实多模态大模型」闭环**的单体原型，
主循环在 `main.py` / `api/websocket_server.py` 里，状态由 `spatial/state_manager.py` 直接
向 `simulator/` 取数并拼装成 `SpatialState`，Agent 在同一轮里同步调用大模型。

分层（改造前）：

```
main.py / api/websocket_server.py   ← 主循环（感知→决策→执行 全挤在一起）
        ↓
spatial/state_manager.py            ← 直接 import simulator，一边取数一边拼状态
        ↓
simulator/  (map / navigation / obstacle / sensor)
        ↓
spatial/spatial_state.py            ← SpatialState（Pydantic）
        ↓
agent/agent_core.py                 ← 决策（同步调大模型 + 规则兜底）
        ↓
agent/tools.py                      ← 副作用出口
```

它的**契约层（`SpatialState`）与安全兜底（工具出口、熔断、规则降级）是扎实的** ——
这两块是 v0.3 得以渐进式演进而非推倒重写的前提（详见审计报告的结论段）。

## 2. 发现的问题

Stage 1 的架构审计（`docs/V02_ARCHITECTURE_AUDIT.md`，**只输出报告、不改代码**）实测确认了
三个 P0，以及一组结构性隐患：

### 2.1 三个 P0（实测数据，均已关闭）

| # | 问题 | 实测证据 | 处置 |
| --- | --- | --- | --- |
| ① | **主循环只有 0.52 Hz**（配置写 1.0） | `sleep(max(0, next_t - elapsed))` 被大模型阻塞吃掉；而**同一轮的纯计算只要 0.21 ms** ⇒ 与算力无关 | **Stage 7 已修**：拆双循环 + 认知工作线程 + LLM 总预算。修复后单轮纯计算实测均值 **0.390 ms**（400 轮） |
| ② | **`localization_confidence` 恒为 0.9** | 该字段是配置常量，低置信度分支是**死代码**（永远进不去） | **Stage 4 已修**：引入置信度估计器，实测定位噪声 1.5 m 时可降到 **0.341**；验收场景 S01 现在会校验该值在 `[0,1]` 区间且随噪声变化 |
| ③ | **单轮 step 最坏 360 s** | `OpenAI` 客户端未设 `max_retries`，**实测默认值为 2** ⇒ 超时叠加 | **Stage 7 已修**：`max_retries=0` + `total_deadline_s` 总闸门（闸门放在**发起请求之前**）；自检用例 62 钉死 |

### 2.2 结构性隐患（不对应单一 P0，但决定了后续每个 Stage 的做法）

- **Agent 每轮盲目看全量状态**，没有「发生了什么」的概念 ⇒ 无法区分「一直如此」与「刚刚变化」。
- **感知与推进世界搅在一起** ⇒ 换硬件要动 Agent。
- **大模型同步阻塞主循环** ⇒ 延迟不可控会直接吃掉安全余量。
- **状态里没有统一的时间/来源/可信度三件套**（Gate 8 的由来）。
- **没有历史数据落盘** ⇒ 实验只能现场跑一次，无法做模型 A/B。

## 3. 实际修改内容

v0.3 严格按任务书第二十七节的分阶段顺序推进（Stage 1 禁止改代码），逐阶段记录见
`docs/V03_MIGRATION_LOG.md`。下表是**新增/重写的模块**（不是全部改动，仅列主干）：

| Stage | 内容 | 新增/重写的主要文件 |
| --- | --- | --- |
| 1 | 架构审计（不改代码） | `docs/V02_ARCHITECTURE_AUDIT.md` |
| 2 | 数据模型：Egocentric Spatial State | `spatial/spatial_state.py`（重写为多子模型契约，含 `timestamp`/`source`/`confidence`） |
| 3 | Sensor Adapter Layer | `sensors/base.py`（`SensorProvider` / `WorldStepper` / `CameraProvider` 三个 Protocol）、`sensors/simulated/` |
| 4 | State Fusion | `fusion/state_fusion.py`、`fusion/confidence.py`、`fusion/freshness.py` |
| 5 | Spatial World Model + 地图接口 | `world_model/`（6 个文件）、`maps/`（5 个文件） |
| 6 | Event Engine | `events/`（5 个文件，16 类事件） |
| 7 | Safety / Cognitive 双循环 | `agent/orchestrator.py`、`agent/safety/`、`agent/llm_client.py`（熔断 + 时间预算） |
| 8 | Interaction Policy + Action Schema | `agent/interaction_policy.py`、`agent/action_schema.py` |
| 9 | Recording / Replay | `recording/`（3 个文件） |
| 10 | Active Perception | `agent/active_perception.py` |
| 11 | 验收收尾 | `tests/acceptance.py`（10 场景）、`tools/acceptance_demo.py`（七拍 Demo）、9 份文档、版本升级 |

**另有本轮直接需求带来的三项改动**（不属于任务书 Stage 序列）：
更大更复杂的室内场景（`config/config.yaml` 数据驱动，代码零改动）、
WASD 手动驾驶、`replans` 计数器虚高修复。

## 4. 新架构

目标链路（任务书第三十六节）已完整落地：

```
Raw Sensors
  │  sensors/base.py 定义的 SensorProvider / CameraProvider（唯一硬件接口面）
  ↓
Structured Observations        sensors/simulated/* → 加噪读数 + 候选枚举
  ↓
State Fusion                   fusion/（融合 + 置信度估计 + 新鲜度）
  ↓
Egocentric Spatial State       spatial/spatial_state.py（带 timestamp/source/confidence）
  ↓
Spatial World Model            world_model/ + maps/（持续记忆，不再每帧重算）
  ↓
Events / Risk / Uncertainty    events/（16 类事件）+ agent/safety/（四级裁决）
  ↓
Agent Reasoning                agent/orchestrator.py（安全快循环 ∪ 认知慢循环）
  ↓
Interaction Decision           agent/interaction_policy.py（表达闸门）
  ↓
Human Action                   agent/tools.py（副作用唯一出口）→ 是否允许前进
  ↓
New Perception
```

关键结构变化：**主循环从「同步调模型」变为「快循环 + 认知线程」**，
装配点从分散收敛到 `SpatialAgentSystem.__init__` 一处（分层铁律由此可被自检强制）。
完整架构图与数据流见 `docs/ARCHITECTURE_V03.md`。

## 5. 关键设计决策

| # | 决策 | 理由 |
| --- | --- | --- |
| 1 | **快循环每轮必跑且不碰网络** | 安全不能依赖延迟不可控的组件。实测 LLM 睡 5 s 时，5 轮 step 仅耗 0.00 s，安全层每轮照常判 `emergency` |
| 2 | **只有真线程能保证「LLM 卡住不丢安全」** | sleep 与网络阻塞都释放 GIL，单线程把 timeout 调多小都没用 |
| 3 | **并发只允许「单写者 + 快照」** | 主线程独占写世界/状态/记忆；交给认知线程的是 `model_copy(deep=True)` 只读快照。唯一例外是工具执行，`AgentTools.call()` 持 `RLock` 且**不跨网络等待** |
| 4 | **事件用边沿检测，首帧只登记不发声** | 否则启动瞬间会把「世界本来就是这样」误报成「刚刚发生了」。且基线标志必须在 `detect()` 末尾统一置位，否则「本轮无物体」时永远建不起基线 |
| 5 | **"多久不重复"绝不用魔法数字** | 初版写 `tick > 2` 跳过启动，导致代码与测试对「首帧该不该报」各说各话 |
| 6 | **配置里的 `null` 必须在读取处兜底** | `safety.critical_distance_m: null` 意为「沿用 `agent.risk` 的口径」，`float(None)` 会直接启动失败 |
| 7 | **库的隐式默认值必须显式覆盖** | openai SDK `max_retries` 默认 2 —— 正是 P0 ③ 的来源 |
| 8 | **新字段绝不用 Pydantic `@computed_field`** | 端到端用例会做 `model_validate(model_dump())` 且模型 `extra="forbid"`，computed_field 进 dump 却不是合法入参 ⇒ 往返报错。改用普通字段 + `model_validator` |
| 9 | **"应所求 ≠ 打扰"** | 用户刚提问的回答被 4 s 最小间隔闸门吞掉（实测症状：问了没反应）。给闸门加 `answering_user` 逃逸口，且该逃逸**只作用于 LLM 输出路径**，规则路径语义逐字不变 |
| 10 | **新能力只加在非确定路径上** | 规则路径必须可证明不变 —— 这是 120 轮零差异能成立的前提，而不是运气 |

## 6. 兼容性

采用**只增不删 + 类名别名 + 字段名不变 + 只读属性转发**的兼容式演进：

- 数据模型只加字段，不删不改名；`PoseState` 保留 `UserState = PoseState` 别名，字段仍叫 `user`，另加只读属性 `pose` 转发。
- v0.2 的老构造写法与老断言**零改动**通过。
- 线上格式冻结：`Action.as_dict()` 的键集被自检用例 64 钉死，新增字段只能走 `to_dict()`。
- 分层铁律（用例 47）全仓扫描 `^\s*(from|import)\s+simulator`：`spatial/state_manager.py`、`agent/tools.py`、`agent/agent_core.py`、`sensors/base.py`、`recording/*` 必须为零，唯一装配点在 `SpatialAgentSystem.__init__`。

**回归证明方式（不看"测试是否变绿"，而看"行为是否逐轮相同"）**：
把 N 轮 `SpatialState.model_dump()` + `Action.as_dict()` 序列化后逐字段 diff，
剔除 `timestamp` / `age_s` / `elapsed`（这些天然每轮不同）。
Stage 3~8 均取得 **规则模式 120 轮 Action 逐轮零差异**。

## 7. 测试结果

| 测试 | 命令 | 结果 |
| --- | --- | --- |
| 细粒度自检 | `python main.py --selftest` | **74 / 74 通过**（耗时约 12.3 s） |
| 验收场景（Gate 1~10） | `python main.py --acceptance` | **10 / 10 通过**（耗时约 1.2 s） |
| 七拍验收 Demo | `python main.py --acceptance-demo` | **7 / 7 拍通过**（转写见 `docs/V03_DEMO_TRANSCRIPT.md`） |
| 前端冒烟 | `tools/ui_smoke_test.py`（需先 `--serve`） | **44+ 项全通过** |
| 端到端（含摄像头链路） | `tools/e2e_test.py` | 全部通过 |
| 录制 → 回放一致率 | 验收场景 S10 | 录制 30 轮 → 回放 **30/30（100%）**，且回放器零 `import simulator` |
| 行为回归 | 行为快照逐轮比对 | 规则模式 **120 轮 Action 逐轮零差异** |

Gate 1~10 覆盖情况（`--acceptance` 会直接打印这张表）：

| Gate | 要求 | 覆盖场景 |
| --- | --- | --- |
| 1 | 项目可以启动 | S01 |
| 2 | 旧 Demo 基本能力还存在 | S01 / S02 / S09 |
| 3 | iPhone 图片仍然可以接收 | S06 / S07（真机上行另见 `tools/e2e_test.py`） |
| 4 | Simulator 可以运行 | S09 |
| 5 | LLM 可以关闭并用 Mock 测试 | 全场景 `enable_llm=False` + S06 / S08 |
| 6 | LLM 失败不会让系统崩 | S08 |
| 7 | Safety Loop 独立于 LLM | S08 |
| 8 | SpatialState 有 timestamp/source/confidence | S01 |
| 9 | Event 可以触发 Agent | S03 / S05 |
| 10 | Replay 可以读取历史数据 | S10 |

七拍 Demo 的实际结果（节选，完整见转写）：
第 1 拍 导航激活（剩余 43.9 m / 路线 7 点）→ 第 2 拍 直行 12.1 m 仅播报 1 条、零重复
→ 第 3 拍 预警档提醒并放行、第 4 轮触发绕行重规划、近距离 `emergency` 停行
→ 第 4 拍 `OBSTACLE_CLEARED` 落地且**新增播报 0 条**
→ 第 5 拍 提问带图 42481 B、回答 13 字
→ 第 6 拍 断流后仍前进 5.5 m、未被误判为安全问题
→ 第 7 拍 LLM 睡 5 s 期间 5 轮 step 仅 0.00 s，危险档 4 轮全部 `emergency` + 禁行、LLM 调用 0 次。

## 8. 性能结果

| 指标 | 实测值 | 说明 |
| --- | --- | --- |
| 单轮纯计算耗时 | 均值 **0.390 ms** / 中位 0.358 ms / 最大 2.196 ms（400 轮） | 配置节拍 1 Hz，占用率约 0.04% |
| 快循环求值次数 | **恰好 1 次/轮**（60 轮 = 60 次；400 轮 = 400 次） | 证明安全层真的每轮都跑 |
| LLM 卡 5 s 对主循环的影响 | 5 轮 step 共 **0.00 s** | 大模型在工作线程里睡，主循环完全不被拖住 |
| 认知结果 TTL | `action_ttl_s = 4.0` | 过期结果丢弃，不"追认" |
| 认知节流 | `heartbeat_s = 5.0` / `min_interval_s = 2.0` | 限制调用频率，省额度 |
| 表达层节流 | `min_interval_s = 4.0`、`max_chars = 40`、`urgent_bypass_interval = true` | 防刷屏；紧急可突破 |
| 400 轮内重规划次数 | **6 次** | `replans` 虚高问题修复后回到合理量级 |
| 一次 60 轮运行的播报量 | **7 条**（其中末句为「已到达目的地」） | 平直路段保持安静（第 2 拍 12 轮仅 1 条） |
| 世界模型规模（60 轮后） | 轨迹点 60 / 已访问区域 8 个 / 物体记忆 16 条 / 事件 24 条 | 记忆是持续的，不是每帧重算 |
| 验收场景整轮耗时 | **1.2 s**（10 个场景） | 全离线、无网络 |

## 9. 当前已知问题

| # | 问题 | 影响 | 处置 |
| --- | --- | --- | --- |
| 1 | 绕行提醒话术里混入英文物品名（如「前方0.6米有**chair**，请慢行」） | 用户可读性 | **未修**。来源是规则路径 `agent/decision.py` 直接取 `obstacle.type.value`。修改会改变确定性快照，违反"规则路径可证明不变"，故**故意留到后续**统一做一层物体名词中文化 |
| 2 | `visual_event_types` 里有两个**永不命中**的预留名（`OBSTACLE_DISAPPEARED` / `NARROW_PASSAGE_DETECTED`） | 配置噪声，易误以为"障碍消失会触发取图" | **已加注释标注**（`config/config.yaml` 与 `agent/active_perception.py`），未删除以保持配置快照稳定 |
| 3 | `EventType` 源码 docstring 曾写「15 类」，实际枚举为 **16** 个成员 | 文档误导 | **已修**（`event_types.py` / `event_engine.py` / `events/__init__.py` 均改为 16） |
| 4 | `on_demand` 感知模式未在**真实多模态链路**（真机 iPhone + 真实模型）上验证过 | 验收场景 S06 用的是替身 | 计划在 v0.4 接真机时补做；v0.3 的替身覆盖已证明逻辑正确 |
| 5 | 取帧请求只做「登记」，没有真正下发给手机端 | 主动感知无法强制手机立刻出帧 | **不在 v0.3 范围**（任务书只要求"请求获取新的 iPhone frame"）；需增加 iOS 侧协议 |
| 6 | 环境提问关键词是小词表（`env_query_keywords`） | 中文口语变体可能漏判为"不是问环境" | 词表在配置里，可随时扩充 |
| 7 | 被挡住后智能体会**原地转身**把障碍甩出前向锥，安全层随即正确判 `ok` | 行为合法（现实中转身找路也是对的），但容易被误读为"安全层失效" | 已在 `docs/TEST_PLAN.md` 与 Demo 转写中写明，并把断言改成更精确的不变式：「只要前方净空进入危险档，就必须 emergency 且禁止前进」 |
| 8 | 前端冒烟用例数在 42~45 之间浮动（含 4 项诊断性检查，视运行时环境计入） | 计数不稳定 | 文档统一表述为「44+ 项」 |

## 10. v0.4 推荐开发方向

一句话：**把"数据来源"换成真实硬件，Agent 与契约层一行不改。**

v0.3 已经把这件事的**唯一接口面**准备好了：`sensors/base.py` 的三个 Protocol
（`SensorProvider` / `WorldStepper` / `CameraProvider`）、以及 `sensors/future/` 的接口预留文件。
v0.4 只需新增 `sensors/future/` 下的实现并在装配处换一行。

优先级建议：

1. **真实 UWB 定位** → `sensors/future/uwb_provider.py`（实现 `SensorProvider`）。风险最高，因为它直接决定位姿与置信度，会牵动世界模型与导航。
2. **智能眼镜 IMU** → 与 UWB 融合解算位姿与步速；同时提供 `health()`，让"UWB 失联"能产生事件。
3. **iPhone LiDAR 深度避障** → 实现 `scan_wall_ahead()` / `scan_sides()`（ARKit 深度图反投影），替换 `simulator/obstacle_simulator.py` 的障碍与净空产出。
4. **UE5 数字孪生** → 可同时实现 `SensorProvider` + `WorldStepper`（孪生既是数据源也是世界），这是唯一允许保留 `debug_ground_truth()` 的实现。
5. **真实语音** → 替换用户指令输入通道（`api` 的 query 通道）与播报出口（`agent/tools.py::speak`），**保持工具签名不变**。
6. **结构化障碍输出** → 让 iPhone「安心行」直接产出结构化障碍状态，绕过视觉模型的延迟。

同时建议在 v0.4 顺手关掉第 9 节的两项：物体名词中文化（问题 1）、物体名词 + 事件名统一词表。

详细接入位置、每个文件要满足的契约、以及"真机与仿真关键差异"，见
**`docs/V04_REAL_SENSOR_INTEGRATION_PLAN.md`**（本文档按要求**只做预留，不实现**）。

---

## 附：交付物清单

| 类别 | 文件 |
| --- | --- |
| 验收证据 | `tests/acceptance.py`（10 场景）、`tools/acceptance_demo.py`（七拍）、`docs/V03_DEMO_TRANSCRIPT.md` |
| 架构文档 | `docs/ARCHITECTURE_V03.md`、`docs/SPATIAL_STATE_SCHEMA.md`、`docs/EVENT_SYSTEM.md`、`docs/SAFETY_ARCHITECTURE.md`、`docs/REPLAY_SYSTEM.md`、`docs/TEST_PLAN.md` |
| 迁移文档 | `docs/V02_ARCHITECTURE_AUDIT.md`、`docs/V03_MIGRATION_LOG.md`、`docs/MIGRATION_V02_TO_V03.md` |
| v0.4 预留 | `docs/V04_REAL_SENSOR_INTEGRATION_PLAN.md`（不实现） |
| 总览 | `README.md` |
| 本报告 | `V0.3_DELIVERY_REPORT.md` |
